# Expected — Project management

## Pass

- Board canvas is `canvas` `#010102`; columns/toolbar are `surface-1` +
  `hairline`.
- Issue cards are `surface-2` `#141516` + `hairline`, 12px radius, 16px padding.
- Issue IDs (`LIN-…`) are mono; titles are `body-sm` 14/500 `ink`.
- Count badges are `surface-2` bg + `ink-muted`, pill radius.
- Drop target uses `hairline-strong`, never a shadow.

## Fail

- Drop shadows on cards.
- Colored column backgrounds (columns must be surface lifts).
- Mono used for marketing chrome (titles, headings).
- Invented priority/label hex values presented as the official professional SaaS palette.
